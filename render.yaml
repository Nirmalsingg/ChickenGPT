services:
  - type: web
    name: chickengpt
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: 18.20.8
      - key: GROQ_API_KEY
        sync: false
